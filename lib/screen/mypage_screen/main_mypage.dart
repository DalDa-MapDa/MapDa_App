import 'package:flutter/material.dart';

class MainMypage extends StatelessWidget {
  const MainMypage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MainMypage'),
      ),
    );
  }
}
